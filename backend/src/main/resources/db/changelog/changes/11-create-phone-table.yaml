databaseChangeLog:
  - changeSet:
      id: create-phone-table
      author: kerrrusha
      changes:
        - createTable:
            tableName: phone
            columns:
              - column:
                  name: id
                  type: BIGINT
                  autoIncrement: true
                  constraints:
                    primaryKey: true
              - column:
                  name: value
                  type: VARCHAR(511)
                  constraints:
                    nullable: false
                    unique: true
              - column:
                  name: user_id
                  type: BIGINT
                  constraints:
                    nullable: false
                    references: user(id)
                    foreignKeyName: fk_phone_user_id
                    deleteCascade: true
        - addUniqueConstraint:
            tableName: phone
            columnNames: user_id, value
            constraintName: unique_phone_user_id_value
      rollback:
        - dropTable:
            tableName: phone