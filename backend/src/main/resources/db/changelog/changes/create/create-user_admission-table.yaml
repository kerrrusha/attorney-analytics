databaseChangeLog:
  - changeSet:
      id: create-user_admission-table
      author: kerrrusha
      changes:
        - createTable:
            tableName: user_admission
            columns:
              - column:
                  name: id
                  type: BIGINT
                  autoIncrement: true
                  constraints:
                    primaryKey: true
              - column:
                  name: value
                  type: VARCHAR(511)
                  constraints:
                    nullable: false
                    unique: true
              - column:
                  name: user_id
                  type: BIGINT
                  constraints:
                    nullable: false
                    references: user(id)
                    foreignKeyName: fk_user_admission_user_id
                    deleteCascade: true
        - addUniqueConstraint:
            tableName: user_admission
            columnNames: user_id, value
            constraintName: unique_user_admission_user_id_value
      rollback:
        - dropTable:
            tableName: user_admission