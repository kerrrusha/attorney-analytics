databaseChangeLog:
  - changeSet:
      id: create-title-table
      author: kerrrusha
      changes:
        - createTable:
            tableName: title
            columns:
              - column:
                  name: id
                  type: BIGINT
                  autoIncrement: true
                  constraints:
                    primaryKey: true
              - column:
                  name: name
                  type: VARCHAR(511)
                  constraints:
                    nullable: false
                    unique: true
              - column:
                  name: user_id
                  type: BIGINT
                  constraints:
                    nullable: false
                    unique: true
                    references: user(id)
                    foreignKeyName: fk_title_user_id
                    deleteCascade: true
              - column:
                  name: deleted
                  type: BOOLEAN
                  defaultValue: false
                  constraints:
                    nullable: false
        - addUniqueConstraint:
            tableName: title
            columnNames: user_id
            constraintName: uk_user_id
      rollback:
        - dropTable:
            tableName: title